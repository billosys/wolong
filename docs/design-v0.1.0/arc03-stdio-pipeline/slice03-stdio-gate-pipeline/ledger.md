# Slice 03 (wolong arc03): stdio-gate-pipeline

> Ledger per `LEDGER-DISCIPLINE.md` v2.0, Section A. Opened 2026-08-26.
> Closer: CC. Verifier: CDC. Evidence names commits and command results; CDC
> upgrades accepted `done` evidence from attested to reproduced.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| SG-1 | Pipeline parser invocation uses the supported complete planning-instance parser call with `--output -`. | inspect parser argv builder/call path; CT fixture asserts parser output target is stdout and domain/problem roles are not both `-` | serious | arc03 slice03 goal | done | Commits `d243a66`, `ea2a330`; `wolong-pipeline:run/2` calls `wolong-gate:run-parser-stdout-to/5`; `stdio_gate_helpers_use_stdin_stdout_and_parser_caveat` passed. | Parser still receives domain/problem paths in the pipeline. |
| SG-2 | Parser stdout bytes are classified as the parser artifact without requiring a parser output file for success. | CT solved path asserts parser detail records stdout artifact bytes/source and status fields | serious | arc03 A3 | done | Commits `d243a66`, `ea2a330`; `classify-stdout/3` and `assert-gate-success/2`; `rebar3 as test ct` passed 73 tests. | Pipeline materializes the captured bytes afterward for workspace evidence. |
| SG-3 | Grounder is invoked with input path `-`, receives parser stdout bytes via `wolong-exec:run-stdin/4`, and receives EOF. | CT fixture blocks until EOF and asserts `path=-` / `path_role=htn` status fields or equivalent marker | serious | arc03 A3 | done | Commits `d243a66`, `ea2a330`; `run-grounder-stage/7` calls `wolong-gate:run-grounder-stdin-to/4`; pipeline CT asserts `path=-`, `path_role=htn`, `operation=read`. | No shell pipe and no file path handoff. |
| SG-4 | Grounder stdout bytes are classified as the grounder artifact without requiring a grounder output file for success. | CT solved path asserts grounder detail records stdout artifact bytes/source and status fields | serious | arc03 A3 | done | Commits `d243a66`, `ea2a330`; `wolong_gate_SUITE:stdio_gate_helpers_use_stdin_stdout_and_parser_caveat/1` asserts no grounder file is required by the gate helper; full CT passed. | Pipeline materializes after classification only. |
| SG-5 | Engine is invoked with input path `-`, receives grounder stdout bytes via `wolong-exec:run-stdin/4`, and receives EOF. | CT fixture blocks until EOF and asserts `path=-` / `path_role=engine_input` status fields or equivalent marker | serious | arc03 A3/A4 | done | Commits `d243a66`, `ea2a330`; `run-engine-stage/6` calls `wolong-gate:run-engine-stdin-to/4`; CT asserts `path=-`, `path_role=engine_input`, `operation=read`. | Release-critical engine boundary is now stdin/stdout. |
| SG-6 | Engine solved stdout bytes become the durable plan payload before workspace cleanup. | CT `keep-artifacts=false` solved public plan still contains plan payload bytes and source metadata | serious | project W1 | done | Commits `d243a66`, `ea2a330`; `attach-plan-payload/1` records `source=engine-stdout`; `keep_false_preserves_payload_after_cleanup` passed. | Public payload survives workspace removal. |
| SG-7 | Engine no-plan with exit `2`, `status=domain_no_plan`, and empty stdout remains `#(domain-no-plan Detail)` internally and public `#(unsolvable Detail)`. | CT internal pipeline and public plan no-plan cases | serious | project W1/A4 | done | Commits `d243a66`, `ea2a330`; `no_plan_keep_true_preserves_domain_result`, `no_plan_returns_unsolvable`, and dispatch public-shape CT passed. | Empty stdout is valid only for engine no-plan. |
| SG-8 | Final machine status remains parsed from stderr for all stdio gates. | CT success and failure fixtures include stderr `PANDAPI_STATUS`; grep/inspect no stdout-status dependency | serious | managed-process safety | done | Commits `d243a66`, `ea2a330`; fixtures emit final `PANDAPI_STATUS` on stderr; `wolong-status:parse/1` call path remains `maps:get 'stderr`; scope grep found no stdout-status classifier. | Diagnostic prose is not parsed. |
| SG-9 | Parser, grounder, and engine failure statuses remain typed and gate-named. | CT parser invalid/missing, grounder invalid, engine invalid, status mismatch/missing, and unmapped status cases | serious | arc03 A5 | done | Commits `d243a66`, `ea2a330`; `rebar3 as test ct` passed parser, gate, pipeline, plan, and dispatch failure suites. | Existing status vocabulary mapping is preserved. |
| SG-10 | Exec-layer failures from stdio handoff remain typed and gate-named. | CT stdin send failure if feasible, invalid stdin via gate helper if exposed, command-not-found, start-failed, and timeout paths | correctness | arc03 A5 | done | Commits `d243a66`, `ea2a330`; `mapper_exec_error_is_typed`, missing-binary public tests, and timeout tests passed; `classify-artifact/3` still wraps exec errors with gate detail. | No feasible public gate path intentionally injects invalid stdin. |
| SG-11 | Timeout cleanup still kills TERM-resistant stdio children and later dispatches recover. | CT engine timeout over stdin, no survivor PID, then solved plan succeeds | serious | project W2/A6 | done | Commits `d243a66`, `ea2a330`; `term_resistant_timeout_has_no_survivor_and_recovers` passed using engine `os-pid`; full CT passed 73 tests. | `result-detail/4` now includes `os-pid` for this verification. |
| SG-12 | Output caps/truncation are surfaced honestly for stdout artifacts and stderr diagnostics. | CT fixture floods stdout/stderr; assert bounded captures and typed result/metadata rather than silent artifact trust | correctness | arc03 OQ4/A6 | done | Commits `d243a66`, `ea2a330`; `stdout_artifact_truncation_is_typed` uses the `output-flood` fixture and asserts `artifact-truncated`, bounded stdout, and truncated stderr metadata. | Chosen behavior: truncated stdout artifact is a typed gate error. |
| SG-13 | Workspace metadata and cleanup remain honest for stdout-sourced artifacts. | CT `keep-artifacts=true` and `keep-artifacts=false`; inspect artifact metadata source/path/bytes and cleanup result | correctness | arc02 compatibility | done | Commits `d243a66`, `ea2a330`; keep-true tests assert materialized parser/grounder/engine files; keep-false public plan retains payload after cleanup and reports `source=stdout`. | Materialized files are provenance/debug artifacts, not handoff inputs. |
| SG-14 | Public `wolong:plan/2,3` shapes are unchanged for solved, unsolvable, typed gate errors, dispatch metadata, and verification-boundary metadata. | existing and new CT in `wolong_plan_SUITE`/`wolong_dispatch_SUITE` | serious | public API compatibility | done | Commits `d243a66`, `ea2a330`; `wolong_plan_SUITE` passed 10 tests and `wolong_dispatch_SUITE` passed 10 tests; full CT passed 73 tests. | Public payload source changes to `engine-stdout`; return tags stay stable. |
| SG-15 | `wolong:validate/2` remains parser-only and does not invoke grounder or engine. | CT parser-only dispatch case and parser validation suite; result shape remains parser validation | correctness | public API compatibility | done | Commits `d243a66`, `ea2a330`; `wolong_dispatch_SUITE:validate_remains_parser_only/1` and `wolong_parser_SUITE` passed. | Validation remains file-backed parser validation and is outside the stdio pipeline proof. |
| SG-16 | CI fixtures model Chengdu stdio artifact behavior honestly, including parser stdout, grounder stdin/stdout, engine stdin/stdout, and parser `- -` unsupported. | inspect fixture scripts and CT assertions | serious | CI honesty | done | Commits `d243a66`, `ea2a330`; fixture scripts support `--output -`, grounder/engine `-` stdin, and parser `- -` usage error; `stdio_gate_helpers_use_stdin_stdout_and_parser_caveat` passed. | Remote CI remains fixture-backed. |
| SG-17 | Optional local real-Chengdu solved and no-plan smoke probes are recorded if sibling binaries are available. | run Wolong pipeline or public plan with `../chengdu/bin/pandapi-*`; record command/result, or reason skipped | correctness | release confidence | done | Local smoke on 2026-08-26 ran public `wolong:plan/3` with sibling Chengdu binaries: minimal returned `#(ok Plan)` with 2040 payload bytes; unsolvable returned `#(unsolvable Detail)` with engine `domain_no_plan`, exit `2`, empty stdout. | Local-only evidence; this does not close Slice04. |
| SG-18 | Scope guard holds: no split parser workers, framed stdin, provisioning, public API redesign, diagnostic-prose classifier, legacy binary fallback, verifier, action parser, or decomposition parser lands. | inspect diff and `rg` for scoped terms in `src`, `test`, `README.md`, and docs | serious | slice-doc scope | done | Commits `d243a66`, `ea2a330`; diff limited to `wolong-gate`, `wolong-pipeline`, gate fixtures, and CT suites; scope grep found only existing public deferral prose and slice docs. | No public API redesign. |
| SG-19 | Local gates and formatter check pass. | `rebar3 compile`; `rebar3 as test eunit`; `rebar3 as test ct`; `rebar3 xref`; `rebar3 dialyzer`; `rebar3 lfe format --check` | correctness | repo workflow | done | 2026-08-26: compile pass; eunit pass 9/0; CT pass 73/0; xref pass; dialyzer pass; `rebar3 lfe format --check` pass 13 files; `rebar3 as test lfe format --check` pass 21 files. | No exceptions. |
| SG-20 | A meaningful tamper cycle proves a new stdio pipeline invariant. | break grounder/engine stdin handoff, EOF, stdout artifact use, status stream, or no-plan empty stdout handling; show CT fail; revert and show pass | serious | ledger discipline | done | Tamper changed grounder handoff back to `run-grounder-to/4` file input; `rebar3 compile && rebar3 as test ct --suite test/wolong_pipeline_SUITE.lfe` failed 4 tests/5 passed; reverted; same command passed 9/0. | Tamper was not committed. |
| SG-21 | Remote CI evidence is recorded honestly. | link green Ubuntu/macOS run, or mark deferred if not pushed/available | correctness | release confidence | done | GitHub Actions build `33038449082` for `ea2a330` passed on `ubuntu-22.04` and `macos-15`: https://github.com/billosys/wolong/actions/runs/33038449082 | Remote CI is fixture-backed and does not use sibling Chengdu. |
| SG-22 | Closing report walks every ledger row and bubbles up pipeline shape, parser caveat, Slice04 readiness, and any Slice05 hardening need. | inspect `closing-report.md` for SG-1 through SG-22 and Bubble-up sections | correctness | project management | done | `closing-report.md` in this slice; arc plan and project plan updated. | CDC writes `cdc-verification.md` later. |

## What Worked

- Keeping `classify/3` file-backed and adding `classify-stdout/3` made the new
  release path explicit without breaking older helper tests or parser
  validation.
- Materializing captured stdout only after classification gave useful
  workspace evidence while preserving the byte-owned stdin handoff.
- The file-handoff tamper failed at the pipeline assertions, proving the tests
  now distinguish stdout/stdin handoff from the old file-backed path.

## Closure

Proposed done by CC on 2026-08-26.
Rows: 22. Done: 22. Deferred: 0. No-op: 0.
