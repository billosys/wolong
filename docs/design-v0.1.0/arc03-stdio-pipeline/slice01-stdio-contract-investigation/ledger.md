# Slice 01 (wolong arc03): stdio-contract-investigation

> Ledger per `LEDGER-DISCIPLINE.md` v2.0, Section A. All rows open at slice
> start, 2026-08-20. Closer: CC. Verifier: CDC. Evidence names commits and
> command results; CDC upgrades accepted `done` evidence from attested to
> reproduced.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| SI-1 | Current Chengdu docs are surveyed for stdin/stdout/stderr, `--output -`, `--status=stderr`, `--status=stdout`, and supported parser/grounder/engine invocation forms. | inspect `../chengdu/docs/reference/cli.md` and `../chengdu/docs/managed-process.md`; record exact lines or excerpts in closing report | serious | arc03 OQ1/OQ2 | done | `closing-report.md` SI-1; docs surveyed at `../chengdu/docs/reference/cli.md` and `../chengdu/docs/managed-process.md`; bubble-up commit `b52c6db`. | Docs support `--output -` and status-stream ownership, but document input operands as paths, not stdin forms. |
| SI-2 | Current local `pandapi-*` binaries are surveyed with `--help`, `--version`, and executable presence checks. | `ls -al ../chengdu/bin`; run all three `--help` and `--version` commands | serious | arc03 dependencies | done | `closing-report.md` SI-2; `ls -al ../chengdu/bin`; all three `--help` and `--version` probes ran against `pandapi-parser`, `pandapi-grounder`, and `pandapi-engine`. | All binaries report `chengdu_version=0.3.0` and `managed_process_contract=0.3.0`. |
| SI-3 | Parser stdin feasibility is proven or blocked: exact supported way to provide domain/problem through stdin is identified, including whether one or both inputs may be `-`. | run focused parser probes; compare exit status, stdout, stderr final status, and output artifact/stream | serious | release process contract | done | Parser file-input stdout probe passed; parser domain `-`, problem `-`, and both `- -` probes each exited `10` with final `status=cli_usage_error`; see `closing-report.md` SI-3. | Parser input stdin is unsupported for all probed HDDL roles. |
| SI-4 | Grounder stdin feasibility is proven or blocked: parser artifact from stdin reaches grounding and can emit grounder artifact to stdout. | run focused grounder probes with `--output -`; inspect stdout artifact bytes and stderr status | serious | release process contract | done | Grounder file-input stdout probe passed; grounder input `-` probe exited `10` with final `status=cli_usage_error`; see `closing-report.md` SI-4. | Grounder input stdin is unsupported. |
| SI-5 | Engine stdin feasibility is proven or blocked: grounded artifact from stdin reaches search and can emit solved plan or no-plan outcome without stdout/status mixing. | run focused engine probes for minimal solved and unsolvable inputs; inspect stdout, stderr, exit code, and status fields | serious | release process contract | done | Engine file-input stdout solved and no-plan probes passed; engine input `-` probe exited `10` with final `status=cli_usage_error`; see `closing-report.md` SI-5. | Engine input stdin is unsupported; valid no-plan remains typed as `domain_no_plan`/exit `2` for file input. |
| SI-6 | A full real-binary stdio chain is proven or blocked for minimal solved input. | shell probe acceptable for investigation: parser stdout -> grounder stdin -> engine stdin, with status on stderr and no diagnostic-prose classification | serious | arc03 A3 | done | Parser stdout -> grounder stdin pipe exited `10`; grounder reported `status=cli_usage_error`; file-mediated stdout-artifact chain solved as a control; see `closing-report.md` SI-6. | Required stdin chain is Chengdu-blocked. |
| SI-7 | A full real-binary stdio chain is proven or blocked for the valid no-plan fixture. | same as SI-6 using unsolvable/circular-precondition fixture; assert engine `domain_no_plan`/exit `2` | serious | project W1 | done | File-mediated unsolvable control reached engine `status=domain_no_plan`, exit `2`, empty plan artifact; stdin chain remains blocked by unsupported input `-`; see `closing-report.md` SI-7. | No-plan semantics are good, but not through required stdin shape. |
| SI-8 | stdout ownership is verified: artifact stdout never mixes with status, diagnostics, progress, statistics, color, or prompts on supervised paths. | inspect stdout/stderr captures from SI-3 through SI-7; run color/status conflict probes if needed | serious | managed-process safety | done | Supervised `--status=stderr --output -` probes emitted artifacts on stdout and final status on stderr; `--status=stdout --output -` conflict probes exited `10`; see `closing-report.md` SI-8. | Stdout ownership is clean for supported output paths. |
| SI-9 | stderr/status behavior is verified: final `PANDAPI_STATUS` remains machine-parseable for success, no-plan, invalid input, and usage/status conflicts. | run representative success and failure probes; parse final status fields manually or with existing parser if convenient | serious | typed gate mapping | done | Success, no-plan, invalid input, and usage/status conflict probes all emitted final `PANDAPI_STATUS`; see `closing-report.md` SI-9. | Classification can stay exit-code plus final status, not diagnostic prose. |
| SI-10 | erlexec/LFE feasibility is assessed for writing stdin and concurrently reading stdout/stderr without shell command strings. | inspect erlexec docs/API or existing deps; run a minimal LFE probe if feasible; record exact API shape or blocker | serious | arc03 OQ3 | done | Inspected `_build/default/lib/erlexec/README.md` and `src/exec.erl`: `stdin`, `{stdin, ...}`, `exec:send/2`, EOF, and separate stdout/stderr delivery are supported; `src/wolong-exec.lfe` currently omits stdin. | erlexec is not the blocker; Wolong runner extension would be needed only after Chengdu supports input stdin or scope changes. |
| SI-11 | Buffering/deadlock/backpressure risk is assessed for large stdout artifacts and stderr diagnostics. | run or design a bounded-output probe; inspect artifact sizes from real fixtures; record whether Slice05 hardening is required | correctness | arc03 OQ4 | done | Real fixture artifacts were small (`parser` about 2.4 KiB, `grounder` 446 B or 262 B, `engine` about 2.0 KiB for solved, 0 B for no-plan); existing `wolong-exec` drains stdout/stderr concurrently with caps. | Resumed stdio work needs explicit bounded-memory versus stream-to-file policy; current artifact size evidence is not a release-scale proof. |
| SI-12 | CI strategy is recommended honestly before Chengdu release artifacts exist. | inspect current CI and fixture strategy; state whether CI uses strict fixtures, optional local real-binary CT, or future release-artifact jobs | correctness | arc03 OQ5 | done | `.github/workflows/build.yml` and `test/fixtures` inspected; CI uses Wolong-owned fixtures and no sibling Chengdu checkout. | Keep remote CI fixture-backed until release artifacts exist; real-binary probes should be optional local or artifact-backed CI jobs. |
| SI-13 | The final decision is explicit: proceed, Wolong-design-needed, or Chengdu-blocked, with re-entry conditions. | closing report contains a named decision section and maps it to later slices or pause condition | serious | arc03 slice01 goal | done | Decision recorded in `closing-report.md`; Arc03/project/README bubble-up committed as `b52c6db`. | Final decision: Chengdu-blocked. |
| SI-14 | Scope fence holds: no production stdio runner, public API shape change, release provisioning, legacy fallback, diagnostic-prose classifier, public verifier, action parser, or decomposition parser lands. | `rg -n` checks in `src`, `test`, `README.md`, and this slice dir; inspect any hits as prose or pre-existing metadata | serious | slice-doc scope | done | `rg -n 'stdio runner|public API|download|provision|checksum|hex.pm|pandaPIparser|pandaPIgrounder|pandaPIengine|diagnostic prose|wolong:verify|action-sequence|decomposition-tree|queue|distributed' src test README.md docs/design-v0.1.0/arc03-stdio-pipeline/slice01-stdio-contract-investigation`; hits were pre-existing public deferrals or slice prose. | No production code changed. |
| SI-15 | Local gates applicable to the committed change pass, including formatter check per current `AGENTS.md`, or exceptions are recorded with re-entry conditions. | `rebar3 compile`; `rebar3 as test eunit`; `rebar3 as test ct`; `rebar3 xref`; `rebar3 dialyzer`; `rebar3 lfe format --check` | correctness | repo workflow | done | 2026-08-20: `rebar3 compile` pass; `rebar3 as test eunit` pass (9 tests); `rebar3 as test ct` pass (62 tests); `rebar3 xref` pass; `rebar3 dialyzer` pass; `rebar3 lfe format --check` pass (13 files). | No exceptions. |
| SI-16 | Closing report walks every ledger row and includes Bubble-up to Arc03/project with any Chengdu pause condition. | inspect `closing-report.md` for SI-1 through SI-16, `Bubble-up to Arc03`, `project`, and decision wording | correctness | project-management | done | `closing-report.md`; bubble-up commit `b52c6db`. | CDC writes `cdc-verification.md` later. |

## What Worked

- Separating shell probes from Wolong implementation kept the slice from
  landing a workaround around an unsupported Chengdu process boundary.
- Running file-input stdout-artifact controls beside stdin-negative probes made
  the finding precise: artifact stdout/status ownership works, input stdin
  does not.
- Inspecting erlexec after Chengdu probes separated the upstream blocker from
  Wolong runner design work.

## Closure

Closed by CC on 2026-08-20 as **Chengdu-blocked**.
Rows: 16. Done: 16. Deferred: 0. No-op: 0.
