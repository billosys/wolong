# Slice 01 (wolong arc01): app-skeleton

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-06. Closer: CC. Verifier: CDC (authenticated
> clone, Linux build+test reproduction in the sandbox, CI runs fetched
> independently, per-row re-walk). Run evidence must link the specific
> run.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| K-1 | `billosys/wolong` exists, public, default branch `main`, first commits carrying the pre-existing planning docs + CLAUDE.md (with the direct-to-main convention recorded); org and visibility correct on first creation. | repo page fetched; `gh api repos/billosys/wolong` or page inspection; CLAUDE.md diff | correctness | slice-doc §2; the chengdu repo-home lesson | done | `gh repo create billosys/wolong --public --source=. --remote=origin --push` (operator-confirmed). `gh api repos/billosys/wolong --jq '{full_name,private,default_branch,owner:.owner.login}'` → `{"full_name":"billosys/wolong","private":false,"default_branch":"main","owner":"billosys"}`. First push carried the pre-existing 3 commits (planning docs + CLAUDE.md); CLAUDE.md's `## Workflow` section (added this slice) records direct-to-main. Strength: attested (CC); CDC to reproduce via authenticated clone. | org-correct at birth — no transfer step this time |
| K-2 | `rebar3 compile` (with the LFE plugin) succeeds from a clean clone with warnings-as-errors; every dep in `rebar.config` pinned to an exact version (no floats, no branch refs); the OTP app metadata is correct (`mod`, `applications` incl. `exec`). | clean-clone build in sandbox (Linux) + CC attest (macOS); read rebar.config + .app.src | serious | slice-doc §2 | done | `rebar3 compile` on macOS (arm64, OTP 28.1.1): clean, `warnings_as_errors` set, erlexec's native `exec-port` built via c++/ei successfully. `rebar.config` deps: `{lfe,"2.2.2"}`, `{erlexec,"2.3.4"}` (hex pkg name is `erlexec`, app name is `erlexec` registering process `exec` — confirmed via `erlexec.app.src` upstream); `{project_plugins,[{rebar3_lfe,"0.5.7"}]}`; test-profile `{ltest,"0.13.11"}` — all exact strings, no `~>`/branch refs. `wolong.app.src`: `{mod,{'wolong-app',[]}}`, `{applications,[kernel,stdlib,erlexec]}`. Strength: attested (CC, macOS); Linux leg covered by K-7's CI run (both legs run `rebar3 compile`) — CDC to reproduce a clean-clone build directly in the sandbox too. | erlexec compiles native code — both platforms matter |
| K-3 | `application:ensure_all_started(wolong)` returns ok with `wolong_sup` alive under the declared strategy and exec's own tree running; `application:stop(wolong)` is clean — zero error/crash reports in the log for the start/stop cycle (asserted by a test, not by eyeball). | run the start/stop test; read the supervisor init | serious | arc-plan §1 | done | `test/unit-wolong-app-tests.lfe`'s `start-stop-clean`: asserts `ensure_all_started` tag is `ok`, `wolong-sup` and `exec` are both registered (not `undefined`), `application:stop` returns `ok`, then asserts a `logger` handler installed at `level => error` captured zero events across the whole start/stop cycle (`ets:tab2list` on the sink is `[]`) — not eyeballed. `wolong-sup:init/1` declares `#M(strategy one_for_one intensity 5 period 10)` with no children (per slice-doc's "empty-but-real" scope). `rebar3 as test eunit` → `12 tests, 0 failures`, exit 0. Strength: attested (CC). | |
| K-4 | `wolong-config` reads the three config keys (`binaries`, `gate-timeouts`, `workdir`) and validates shape/presence with a **distinct typed error per failure mode** (missing key, wrong shape, non-string path); happy path returns a validated config term; `config/sys.config` example parses and passes. No invented defaults for binary paths. | tests exercise happy + every error path; CDC calls the error paths directly from a shell | serious | arc-plan OQ3 (resolved default); project API contract | done | `test/unit-wolong-config-tests.lfe`: 9 tests — happy path; `missing-key` × 3 (`binaries`/`gate-timeouts`/`workdir`); `wrong-shape` × 3 (binaries not a map, negative gate-timeout, non-boolean `keep-artifacts`); `non-string-path` × 2 (non-string binary path, non-string `base-dir`). All typed as `#(error #(config Reason ...))`, no stringly errors. `binaries` has no default — `application:get_env` returning `undefined` is `missing-key`, full stop. `config/sys.config` verified directly: `file:consult` + `application:set_env` + `apply('wolong-config', validate, [])` → `#(ok #M(...))` with all three keys populated. `rebar3 as test eunit` → `12 tests, 0 failures`. Strength: attested (CC); CDC to exercise error paths directly from a shell per slice-doc §4. | the typed-error discipline starts here |
| K-5 | The erlexec probe: a supervised, synchronous run of a trivial OS command from LFE returns a matched `#(ok ...)`; a nonexistent-command run returns a matched `#(error ...)` (typed, not crashed); **the OQ1 ergonomics verdict (direct calls vs thin wrapper) is recorded in the arc-plan via a tracked change**, whatever it is. | run both probe tests; read the arc-plan Version History entry | serious | arc-plan OQ1 (owned by this slice) | done | `test/unit-wolong-exec-probe-tests.lfe`: `run-trivial-command-ok` asserts `(exec:run "true" '(sync stdout stderr))` tags `ok`; `run-nonexistent-command-errors` asserts a nonexistent command tags `error` (no crash, `erlexec`'s own supervision — process `exec` — stays up). OQ1 verdict recorded in `arc-plan.md` v1.1 (2026-08-06, "surfaced by slice01"): **direct `exec:run/2` calls, no wrapper macro** — option lists and 2-tuple returns are ergonomically plain from LFE as-is. `rebar3 as test eunit` → `12 tests, 0 failures`. Strength: attested (CC). | an unrecorded verdict fails this row even if the code works — recorded, see arc-plan §6 v1.1 |
| K-6 | The test suite is real and falsifiable: covers K-3/K-4/K-5's claims; runs green via rebar3 locally and in CI; and a tamper test (invert one assertion) demonstrably fails the suite with nonzero exit before being reverted. | run suite; perform tamper; revert; re-run | correctness | house vacuous-test rule | done | Canonical test command is `rebar3 as test eunit` (not `rebar3 lfe ltest` — that runner prints a report but does not set a nonzero process exit code on failure, discovered and worked around this slice; `ltest`'s `deftest` still compiles to standard eunit `_test/0` functions, so `rebar3 eunit` runs them natively with correct exit-code semantics). Tamper: inverted `validate-missing-binaries`'s expected atom to `TAMPERED-not-binaries` → `rebar3 as test eunit` → `12 tests, 1 failures`, `===> Error running tests`, exit code 1 (verified via direct `$?`, not through a pipe). Reverted → `12 tests, 0 failures`, exit code 0. Local green run covers K-3/K-4/K-5; CI green run (K-7) covers both platforms. Strength: attested (CC); CDC to re-run the tamper cycle per slice-doc §4. | |
| K-7 | CI green on one linked run: `ubuntu-22.04` + `macos-15`, pinned BEAM-setup action major, pinned OTP/rebar3/LFE versions (surveyed live, choice + rationale in Evidence), steps compile → test → xref → dialyzer, all clean; actionlint zero findings. | run page fetched independently; actionlint locally; read pins | serious | slice-doc §2 | open | | dialyzer exclusions, if any, individually justified inline |
| K-8 | README stub current: the contract paragraph (incl. validated-plan-or-unsolvable), status, dev setup pointing at chengdu `v0.1.0`'s 4-command install for binaries (link), CI badge. CLAUDE.md updated with the recorded workflow convention. | read README/CLAUDE.md at HEAD; badge renders | polish | slice-doc §2 | open | | |

## What Worked

_(At slice close.)_

## Closure

_Open. Rows: 8. Done: 0. Deferred: 0. No-op: 0._
