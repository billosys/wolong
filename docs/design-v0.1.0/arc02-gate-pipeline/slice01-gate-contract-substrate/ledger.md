# Slice 01 (wolong arc02): gate-contract-substrate

> Ledger per `LEDGER-DISCIPLINE.md` v2.0, Section A. All rows open at
> slice start, 2026-08-14. Closer: CC. Verifier: CDC. Evidence must name the
> commit and include command output or direct artifact pointers; CDC upgrades
> accepted `done` evidence from attested to reproduced.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| G-1 | The current Chengdu managed contract for parser, grounder, engine, and the supervised pipeline is surveyed from docs, fixture contract records, and local `../chengdu/bin/pandapi-*` binaries where available. | read ledger/closing survey; inspect references to `../chengdu/docs/reference/cli.md`, `../chengdu/docs/managed-process.md`, and `../chengdu/fixtures/contract/*`; if local binaries exist, run `--version` plus minimal/no-plan probes | serious | arc02 dependencies; slice-doc section 3 | done | Read `../chengdu/docs/reference/cli.md`, `../chengdu/docs/managed-process.md`, and `../chengdu/fixtures/contract/{parser,grounder,engine,pipeline}-contract-records.md`. Local binary probes on 2026-08-14: all three `--version` commands exited 0 with `chengdu_version=0.3.0` and `managed_process_contract=0.3.0`; minimal supervised pipeline exited parser/grounder/engine `0/0/0` with artifacts `2444/446/2043` bytes and empty stdout; unsolvable path exited parser/grounder/engine `0/0/2` with engine final status `domain_no_plan`, `outcome=no_plan`, and no plan artifact. | Real-binary evidence is local-only; CI evidence uses checked-in Wolong fixtures. |
| G-2 | Binary lookup resolves `parser`, `grounder`, and `engine` from the configured app env only, preserving typed missing, non-executable, and stat-failed errors. | `rebar3 compile`; inspect `src/wolong-binaries.lfe`; CT cases for each component and missing/non-executable paths; grep for PATH/env fallback | serious | arc01 OQ3; arc02 slice01 scope | done | Commit `5f0e926` extends `src/wolong-binaries.lfe` with `parser/0`, `grounder/0`, and `engine/0` wrappers over app-env `resolve/1`. `test/wolong_gate_SUITE.lfe` covers all-component resolution, missing `grounder`, and non-executable `grounder`; `test/wolong_parser_SUITE.lfe` preserves parser locator cases. `rg -n "os:getenv" src test ...` returned no implementation/test hits. | `wolong-exec` may resolve non-path commands for the generic runner, but managed binary lookup is app-env-only. |
| G-3 | The final `PANDAPI_STATUS` parser is shared outside `src/wolong.lfe`, preserves required and unknown fields, and has tests for valid records, missing records, malformed fields, numeric exit code parsing, and multiple-line stderr. | inspect new shared module; `rg -n "PANDAPI_STATUS|parse-status|status-fields" src test`; run EUnit/CT parser cases | correctness | arc02 OQ2; arc01 parser implementation | done | Commit `5f0e926` adds `src/wolong-status.lfe`; `src/wolong.lfe` no longer contains the status parser. `test/wolong_gate_SUITE.lfe` covers valid records, missing records, malformed fields, numeric `exit_code`, final-record selection across multiline stderr, and unknown field preservation. `rebar3 as test ct` passed 33 tests locally and in CI run `31828476858`. | The parser uses the final `PANDAPI_STATUS` line, matching the managed contract. |
| G-4 | Parser validation continues to return the arc01 public shapes after shared extraction: success, missing file, output unavailable, invalid-HDDL with `invalid-kind=undistinguished`, timeout, and exec errors. | `rebar3 as test ct`; inspect `test/wolong_parser_SUITE.lfe`; direct call to `wolong:validate/2` if needed | serious | regression guard | done | `src/wolong.lfe` is now a compatibility adapter over `wolong-gate:run-parser/4`. `test/wolong_parser_SUITE.lfe` covers success, missing file, output unavailable, invalid-HDDL with `invalid-kind=undistinguished`, parser timeout, and child-without-status handling; `test/wolong_gate_SUITE.lfe` directly covers true runner `exec` error classification. Local `rebar3 as test ct`: all 33 tests passed. | A bad-interpreter child exits without managed status, so the observable public result is `parser status-missing`; true runner `exec` errors are preserved by the shared gate adapter. |
| G-5 | Shared gate mapping covers current managed statuses and exit families without scraping prose: `ok`, `domain_no_plan`, `cli_usage_error`, `input_unavailable`, `output_unavailable`, `input_invalid`, policy surfaces, timeout/resource/interrupted, dependency/child/internal, signal/no-status, and unmapped status. | inspect mapper clauses/tests; `rg -n "domain_no_plan|cli_usage_error|input_unavailable|output_unavailable|input_invalid|unsupported_feature|legacy_surface|experimental_surface|future_surface|resource_limit|dependency_failure|internal_error|diagnostic" src test` | correctness | Chengdu managed-process docs; project W3 | done | Commit `5f0e926` adds `src/wolong-gate.lfe` classification clauses for the managed vocabulary and exit families. `mapper_covers_managed_status_vocabulary`, `mapper_exec_error_is_typed`, `engine_domain_no_plan_success_shape`, and `engine_domain_no_plan_distinct_from_failures` passed in local CT and CI run `31828476858`. | Classification uses exit code plus final `PANDAPI_STATUS`; diagnostic text is not parsed. |
| G-6 | Gate argv helpers or equivalent call sites build supervised, file-backed argv lists for parser, grounder, and engine: `--supervised --status=stderr --output PATH ...`; no shell command string is constructed. | inspect call sites; `rg -n "wolong-exec:run|--supervised|--status=stderr|--output|os:cmd|/bin/sh|open_port|exec:run" src test` | serious | Chengdu CLI docs; arc01 runner contract | done | `src/wolong-gate.lfe` exposes `parser-argv/3`, `grounder-argv/2`, and `engine-argv/2`; `run-gate/5` calls `wolong-exec:run` with argv lists. Scope grep found no production `os:cmd`, `open_port`, shell-command construction, or managed fallback; `/bin/sh` appears only in fixture scripts. | Production calls remain file-backed and supervised. |
| G-7 | Common Test proves a one-shot supervised parse -> ground -> solve fixture flow through Wolong code, asserting artifact creation, stdout ownership, final status fields for all three components, and solved engine success. | `rebar3 as test ct`; inspect new/updated `*_SUITE.lfe`; inspect `test/fixtures/gate-contract-substrate/` | serious | arc02 A2 | done | `test/wolong_gate_SUITE.lfe:supervised_parse_ground_solve_fixture/1` resolves all three configured fixture binaries, runs parser -> grounder -> engine through `wolong-gate`/`wolong-exec`, asserts artifact creation, empty engine stdout, final `status=ok` fields for parser/grounder/engine, and engine `outcome=solved`. Local and CI CT passed. | This is intentionally a one-shot substrate fixture, not public `wolong:plan/verify`. |
| G-8 | Engine `domain_no_plan`/exit `2` is represented in shared mapping with a success-shaped unsolvable/domain outcome for engine gate use, and a fixture/test proves the mapper distinguishes it from malformed input, timeout, and missing artifact. | `rebar3 as test ct`; inspect engine/no-plan mapper test; compare against Chengdu managed-process docs and engine contract records | serious | project invariant; arc02 A3 | done | `src/wolong-gate.lfe` maps engine `exit=2,status=domain_no_plan` to `#(domain-no-plan Detail)`, not `#(error ...)`. CT cases assert no-plan success shape and distinguish it from `input_invalid`, runner timeout, and missing artifact. Tamper cycle changed this mapping to error-shaped and `rebar3 as test ct --suite test/wolong_gate_SUITE.lfe` failed at `engine_domain_no_plan_success_shape`; reverting restored all 13 gate-suite tests. | Public `#(unsolvable ...)` remains deferred to the later planning API slice. |
| G-9 | CI fixture strategy is honest: checked-in fixture executables may emulate parser/grounder/engine status behavior, but the ledger and closing report do not claim remote CI ran real Chengdu binaries unless it did. | inspect fixtures; inspect ledger/closing report; inspect CI run evidence | correctness | arc01 deferral; arc02 OQ3 | done | CI run `31828476858` ran checked-in fixtures on Ubuntu 22.04 and macOS 15, passing compile, EUnit, CT, xref, and Dialyzer. Real Chengdu binary evidence is recorded only as a local survey in G-1 and closing report, not as CI evidence. | Real binary provisioning remains outside this slice. |
| G-10 | Scope fence holds: no public `wolong:plan`, no public `wolong:verify`, no `gen_statem`, no dispatch supervisor, no Chengdu release downloader, and no legacy `pandaPI*` fallback. | `rg -n "defun plan|defun verify|gen_statem|dispatch|download|release|pandaPIparser|pandaPIgrounder|pandaPIengine" src test docs/design-v0.1.0/arc02-gate-pipeline/slice01-gate-contract-substrate` | correctness | slice-doc section 5 | done | Scope grep after implementation found forbidden terms only in slice/prompt/ledger prose, not in `src/` or `test/`. No public exports were added to `src/wolong.lfe` beyond existing `validate/2`. | Arc02 sequencing preserved. |
| G-11 | Local gates pass: `rebar3 compile`, `rebar3 as test eunit`, `rebar3 as test ct`, `rebar3 xref`, and `rebar3 dialyzer`. | run listed commands and record summaries | correctness | repo workflow | done | Local gates on 2026-08-14: `rebar3 compile` passed; `rebar3 as test eunit` passed 9 tests; `rebar3 as test ct` passed 33 tests; `rebar3 xref` passed; `rebar3 dialyzer` exited 0. CI run `31828476858` passed the same workflow on Ubuntu 22.04 and macOS 15 for commit `5f0e926f718c9a89da57718dfd25702fbdbdc6b5`. | `rebar3 as test ltest` remains not a registered command; ltest-backed unit modules run through EUnit per the existing rebar3_lfe/EUnit autoexport workaround. |
| G-12 | A tamper cycle proves the new gate-contract tests are meaningful: break one status mapping or argv assertion, observe the owning test gate fail nonzero, revert, and observe it pass. | local tamper transcript in ledger/closing report | correctness | ledger discipline | done | Tamper: changed engine `domain_no_plan` classification from `#(domain-no-plan Detail)` to `#(error #(domain-no-plan Detail))`. `rebar3 as test ct --suite test/wolong_gate_SUITE.lfe` failed nonzero with `engine_domain_no_plan_success_shape` failed; after reverting the tamper, the same suite passed all 13 tests, and full CT later passed all 33 tests. | Tamper targeted the new gate substrate invariant directly. |

## What Worked

- Keeping `wolong:validate/2` as a thin compatibility adapter let the shared
  gate substrate move without changing the closed Arc01 public parser shapes.
- The real Chengdu binary probe caught the current engine no-plan spelling and
  exit behavior before implementation, so the mapper uses `domain_no_plan` as
  the authoritative status.
- The rebar3_lfe/EUnit autoexport workaround remains the practical local unit
  path: `rebar3 as test ltest` is not a registered provider, while
  `rebar3 as test eunit` runs the ltest-backed unit tests.

## Closure

Closed at commit `5f0e926f718c9a89da57718dfd25702fbdbdc6b5` plus this
close-out documentation update on 2026-08-14. Verified by: CC local gates and
GitHub Actions run `31828476858`; CDC verification remains separate.
Rows: 12. Done: 12. Deferred: 0. No-op: 0.
