# Slice 05 (wolong arc02): verification-boundary

> Ledger per `LEDGER-DISCIPLINE.md` v2.0, Section A. All rows open at
> slice start, 2026-08-15. Closer: CC. Verifier: CDC. Evidence names the
> implementation commit where applicable; CDC upgrades accepted `done` evidence
> from attested to reproduced.

Implementation commit:
`b33ee7ec5e7f0f84ed97d94e35d2c92bae47abf5`
(`Resolve verification boundary docs`).

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| VB-1 | The current Chengdu 0.3.0 pre-release supported managed surfaces are surveyed from docs and local binaries before changing Wolong's public boundary. | inspect `../chengdu/docs/reference/cli.md`, `../chengdu/docs/managed-process.md`, and `ls -al ../chengdu/bin`; run `../chengdu/bin/pandapi-parser --help`, `../chengdu/bin/pandapi-grounder --help`, and `../chengdu/bin/pandapi-engine --help` if usable locally | serious | arc-plan OQ1 | done | Before edits, `ls -al ../chengdu/bin` showed only executable `pandapi-parser`, `pandapi-grounder`, and `pandapi-engine`. `cli.md` supported-surfaces table names only those three normal surfaces. `managed-process.md` describes the supervised parser -> grounder -> engine pipeline. All three `--help` probes exited 0 and identified only parser, grounder, and engine supported surfaces; all three `--version` probes exited 0 with `managed_process_contract=0.3.0`. | No supported verifier surface was found, so the deferral path was used. |
| VB-2 | Arc02 OQ1 is dispositioned in `arc-plan.md` with a dated Version History entry naming slice05 as the source. | run separate `rg -n` checks for `OQ1`, `verification boundary`, `wolong:verify`, `slice05`, `Version history`, and the new version marker in `docs/design-v0.1.0/arc02-gate-pipeline/arc-plan.md` | serious | project-management bubble-up discipline | done | Commit `b33ee7e`: `arc-plan.md` marks OQ1 `RESOLVED - verifier deferred`, names slice05, distinguishes historical five-gate context from current Arc02 surface, and adds v1.5 version history. `rg -n 'OQ1|verification boundary|wolong:verify|slice05|Version history|v1\\.5' docs/design-v0.1.0/arc02-gate-pipeline/arc-plan.md` returned the expected OQ1, slice05, and v1.5 hits. | Current support is parser, grounder, engine; verifier is deferred. |
| VB-3 | `project-plan.md` states the 0.1.0 implemented public surface honestly: `validate`, `plan`, explicit verification-boundary metadata, and deferred `verify` unless a supported verifier contract exists. | run separate `rg -n` checks for `wolong:validate`, `wolong:plan`, `wolong:verify`, `verification-boundary`, `separate-verifier`, `deferred`, `re-entry`, and `Version history` in `docs/design-v0.1.0/project-plan.md` | serious | project DoD honesty | done | Commit `b33ee7e`: `project-plan.md` now lists implemented `plan/2`, `plan/3`, parser-only `validate/2`, explicit `verification-boundary`, deferred `wolong:verify`, and a re-entry condition. `rg -n 'wolong:validate|wolong:plan|wolong:verify|verification-boundary|separate-verifier|deferred|re-entry|Version history|v1\\.4' docs/design-v0.1.0/project-plan.md` returned the expected boundary hits. | Historical context remains qualified. |
| VB-4 | `README.md` no longer claims the implemented sequence is parse -> ground -> solve -> convert -> verify. | run separate absence checks in `README.md` for the stale five-gate current-surface phrases `parse -> ground -> solve -> convert -> verify` and `parse` followed later by `convert` followed later by `verify` | serious | stale README finding | done | Commit `b33ee7e`: README states the current chain is `pandapi-parser -> pandapi-grounder -> pandapi-engine`; the historical note avoids the stale current-surface phrase. `rg -n 'parse -> ground -> solve -> convert -> verify' README.md` and `rg -n 'parse.*convert.*verify' README.md` both exited 1 with no matches. | Historical conversion/verification context is described without the stale sequence claim. |
| VB-5 | `README.md` reflects current project status and public API: `validate` and `plan` exist, `verify` is deferred, and arc02/arc03 status is current. | run separate positive checks in `README.md` for `wolong:validate`, `wolong:plan`, deferred `verify`, `verification-boundary`, `arc02`, `arc03`, and `provision`; run separate absence checks for stale `No plan`, `No validate`, and `arc01-exec-substrate in progress` wording | correctness | stale README finding | done | Commit `b33ee7e`: README lists `wolong:validate/2`, `wolong:plan/2`, `wolong:plan/3`; states `wolong:verify` is deferred; names `verification-boundary`; says arc01 is closed, arc02 has implemented the boundary disposition, and arc03 provisioning is future. Positive `rg` returned expected hits; stale absence grep `rg -n 'No `?plan|No `?validate|arc01-exec-substrate in progress' README.md` exited 1. | README is now useful for a current developer. |
| VB-6 | `README.md` and dev setup use current `pandapi-*` binary names and do not instruct users to use legacy `pandaPI*` names. | run separate positive checks in `README.md` for `pandapi-parser`, `pandapi-grounder`, and `pandapi-engine`; run separate absence checks for `pandaPIparser`, `pandaPIgrounder`, and `pandaPIengine` | correctness | binary rename correction | done | Commit `b33ee7e`: README dev setup points at `../chengdu/bin/pandapi-parser`, `../chengdu/bin/pandapi-grounder`, and `../chengdu/bin/pandapi-engine`. Positive `rg` returned all three current names; legacy-name absence check in README returned no matches. | Arc03 still owns release provisioning. |
| VB-7 | Public solved plans still carry `verification-boundary.separate-verifier=not-run` and action/decomposition deferrals. | inspect `src/wolong.lfe`; run `rebar3 as test ct --suite test/wolong_plan_SUITE.lfe` and/or a new focused CT assertion | serious | slice03/slice04 invariant | done | Source inspection shows `verification-boundary/0` returns `separate-verifier 'not-run`, `action-sequence 'deferred`, and `decomposition-tree 'deferred`. `rebar3 as test ct --suite test/wolong_plan_SUITE.lfe` passed 10/10 after the tamper revert. Full CT also passed 62 tests. | Existing public `#(ok Plan)` shape preserved; no code changes landed in `b33ee7e`. |
| VB-8 | No public `wolong:verify` function or export lands on the deferral path. | run separate absence checks in `src` and `test` for `defun verify`, exported `verify`, and direct `wolong:verify`; inspect `src/wolong.lfe` export form | serious | arc-plan slice05 | done | Commit `b33ee7e` is docs-only; `src/wolong.lfe` exports only `plan/2`, `plan/3`, and `validate/2`. `rg -n 'defun verify' src test` and `rg -n '\\(verify [0-9]|wolong:verify' src test` both exited 1 with no matches. | Deferral path held. |
| VB-9 | No action-sequence parser or decomposition-tree parser lands; current fields remain explicit deferrals. | inspect `src` and `test`; run separate `rg -n` checks for `action-sequence` and `decomposition-tree`; verify hits are only boundary metadata/assertions, not parser implementation | serious | slice03 deferral | done | `rg -n 'action-sequence|decomposition-tree' src test` returned only `src/wolong.lfe` boundary metadata and `test/wolong_plan_SUITE.lfe` assertions that both fields are `deferred`. No parser implementation landed. | Durable engine payload remains bytes/artifact metadata. |
| VB-10 | No diagnostic-prose classifier lands; all planner outcome classification remains exit/status-field based. | run separate absence checks in `src` and `test` for `diagnostic prose`, `grep` over `stderr`, `stderr` prose matching, and `re:run` over `stderr`; inspect any hits | serious | managed-process contract | done | Commit `b33ee7e` is docs-only. `rg -n 'diagnostic prose|grep.*stderr|stderr.*grep|re:run.*stderr|stderr.*re:run' src test` exited 1 with no matches. Existing classification remains exit/status-field based. | Human diagnostics remain bounded metadata only. |
| VB-11 | No release provisioning, downloader, checksum verifier, hex packaging, or legacy binary fallback is added. | run separate absence checks for `download`, `provision`, `checksum`, `SHA256`, `hex.pm`, `pandaPIparser`, `pandaPIgrounder`, and `pandaPIengine` in `src`, `test`, and `README.md`; inspect this slice directory and verify any hits are out-of-scope prose only | serious | scope fence | done | `rg -n 'download|provision|checksum|SHA256|hex\\.pm|pandaPIparser|pandaPIgrounder|pandaPIengine' src test README.md` returned only README statements assigning release download/checksum/provisioning to Arc03. No source/test provisioning, downloader, checksum verifier, hex packaging, or legacy fallback landed. | Planning prose may mention out-of-scope work; implementation does not add it. |
| VB-12 | Existing public behavior remains green: solved, no-plan, typed failures, dispatch supervision, and parser-only validation still pass. | `rebar3 as test ct`; inspect `wolong_plan_SUITE`, `wolong_dispatch_SUITE`, and `wolong_parser_SUITE` output | serious | regression guard | done | `rebar3 as test ct` passed 62 tests, including `wolong_dispatch_SUITE: ..........`, `wolong_parser_SUITE: .........`, and `wolong_plan_SUITE: ..........`. | Slice03/Slice04 behavior stayed green. |
| VB-13 | Local gates pass: `rebar3 compile`, `rebar3 as test eunit`, `rebar3 as test ct`, `rebar3 xref`, and `rebar3 dialyzer`. | run listed commands | correctness | repo workflow | done | Local gates passed: `rebar3 compile` clean rerun exited 0; `rebar3 as test eunit` passed 9 tests, 0 failures; `rebar3 as test ct` passed 62 tests; `rebar3 xref` exited 0; `rebar3 dialyzer` exited 0 after success typing analysis. | An initial compile/eunit load printed a transient BEAM `not_purged` report but exited 0; clean compile rerun and all gates passed. |
| VB-14 | CI is green on Ubuntu and macOS for the implementation/close commits. | record GitHub Actions run URL and matrix results | correctness | repo workflow | done-local; ci-not-triggered | The implementation commit `b33ee7e` is local on `main` ahead of `origin/main`; CC did not push, so no new GitHub Actions run exists yet. Local gates passed as VB-13 evidence. | Remote CI must be recorded after push; CI uses Wolong fixtures, not sibling `../chengdu`. |
| VB-15 | A tamper cycle proves the boundary checks are meaningful: make a verifier-claim regression, observe the owning test/grep fail nonzero, revert, and observe it pass. | tamper `separate-verifier` to a non-`not-run` value, add a dummy public `verify`, or reintroduce stale README five-gate wording; run the owning check; revert and rerun | correctness | ledger discipline | done | Tamper changed `src/wolong.lfe` `separate-verifier` from `not-run` to `skipped`; `rebar3 as test ct --suite test/wolong_plan_SUITE.lfe` failed nonzero at `solved_plan3_returns_public_plan` with `{expected,'not-run',actual,skipped}`. After revert, the same suite passed 10/10. | Tamper targeted the runtime boundary metadata. |
| VB-16 | Closing report walks every ledger row and includes Bubble-up to the arc with OQ1 disposition, arc-close readiness, and scope-as-specified vs. scope-as-delivered. | run separate `rg -n` checks in `closing-report.md` for each row ID from `VB-1` through `VB-16`, plus `Bubble-up to the arc`, `OQ1`, `arc close`, and `scope-as-specified` | correctness | project-management | done | `closing-report.md` added in the close commit walks VB-1 through VB-16 and includes `Bubble-up to the arc` with OQ1, arc close readiness, Arc03 provisioning evidence, and scope-as-specified vs. scope-as-delivered. | CDC writes `cdc-verification.md` later. |

## What Worked

- Treating the Chengdu survey as the first gate kept this slice on the
  documentation/deferral path instead of quietly inventing a verifier API.
- The existing `wolong_plan_SUITE` assertions already made
  `separate-verifier=not-run` falsifiable, so no new runtime code was needed.
- Exact README greps caught stale status/API wording while still allowing
  historical design context to remain qualified.

## Closure

Closed by CC on 2026-08-15.
Rows: 16. Done: 15. Done-local/CI-not-triggered: 1. Deferred: 0. No-op: 0.
CDC verification remains separate; no `cdc-verification.md` was created by CC.
