(define (domain wolong-unsolvable)
  (:requirements :typing)
  (:types item)
  (:predicates (ready) (blocked))
  (:action mark-ready
    :parameters ()
    :precondition (blocked)
    :effect (ready)))
