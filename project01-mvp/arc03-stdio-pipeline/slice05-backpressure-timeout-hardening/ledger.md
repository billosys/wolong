# Slice 05 (wolong arc03): backpressure-timeout-hardening

> Ledger per `LEDGER-DISCIPLINE.md` v2.2, Section A. Opened 2026-08-28.
> Closer: CC. Verifier: CDC. Evidence names commits and command results; CDC
> upgrades accepted `done` evidence from attested to reproduced.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| BH-1 | Slice05 starts from the active planning worktree, reads Slice04 close/CDC evidence, and preserves the current public API contract: `#(ok Plan)`, `#(unsolvable Detail)`, or typed `#(error #(Gate Reason Detail))`. | inspect `closing-report.md`; inspect `cdc-verification.md`; run existing public plan CT after implementation | serious | arc03 sequence | done | Read active planning worktree, Slice04 close report, and CDC verification. `rebar3 as test ct` passed 87/0 after implementation commit `b34f6bc`. | No public API redesign. |
| BH-2 | The output-capture policy is explicit in code and operator docs. | inspect `src`, `README.md`, `config/sys.config`, and close report for stdout artifact vs stderr diagnostic/status policy | serious | arc03 OQ4/A6 | done | `b34f6bc` documents stdout as artifact channel and stderr as bounded diagnostic/status channel in `README.md`, `config/sys.config`, `wolong-exec`, `wolong-gate`, and close report. | Stderr preview and status tail are bounded separately. |
| BH-3 | Existing `wolong-exec:run/3` and `wolong-exec:run-stdin/4` callers that pass `output-limit-bytes` remain compatible. | CT existing exec suite; grep call sites; add compatibility CT if runner opts change | serious | backward compatibility | done | Existing `output-limit-bytes` remains required and defaults both streams when per-stream limits are absent. `rebar3 as test ct` passed `wolong_exec_SUITE` as part of 87/0. | New keys are optional. |
| BH-4 | Stdout and stderr can be bounded independently, or an equally explicit implemented policy justifies one shared bound. | CT with different stdout/stderr limits; inspect runner/gate option derivation | serious | release-scale policy | done | `wolong_exec_SUITE` asymmetric cap cases passed with stdout 25 bytes and stderr 40 bytes; `wolong-exec` accepts optional `stdout-limit-bytes`/`stderr-limit-bytes`. | `output-limit-bytes` remains fallback. |
| BH-5 | App-env output policy is optional and validated when present. | EUnit or CT config tests for absent policy, valid policy, wrong shape, non-positive limits, and non-string/path-independent values as applicable | correctness | config surface | done | `unit-wolong-config-tests` passed 13/0, covering absent `output-limits`, valid policy, wrong shape, non-positive limit, and unknown stream. | No new required config key. |
| BH-6 | Gate-level runner options are derived per gate from the output policy. | CT or inspection shows parser, grounder, and engine receive the intended stdout/stderr limits | correctness | gate hardening | done | `wolong-gate:runner-opts` derives parser/grounder/engine stream caps from validated `output-limits`; Slice05 CT asserts intended per-gate limits through public results. | No 65536-only gate path remains for configured streams. |
| BH-7 | A parser stdout artifact larger than the old 65536-byte cap but within the configured parser stdout limit can feed grounder stdin successfully. | CT fixture case through `wolong-pipeline` or public `wolong:plan/2,3` | serious | parser stdout artifact scale | done | `wolong_backpressure_SUITE:parser_large_stdout_feeds_grounder` passed; parser stdout bytes exceed 65536, are not truncated, and public planning succeeds. | Full public dispatch path. |
| BH-8 | A grounder stdout artifact larger than the old 65536-byte cap but within the configured grounder stdout limit can feed engine stdin successfully. | CT fixture case through `wolong-pipeline` or public `wolong:plan/2,3` | serious | grounder stdout artifact scale | done | `wolong_backpressure_SUITE:grounder_large_stdout_feeds_engine` passed; grounder stdout bytes exceed 65536, are not truncated, and engine returns a public plan. | Proves middle pipe. |
| BH-9 | An engine solved plan larger than the old 65536-byte cap but within the configured engine stdout limit returns public `#(ok Plan)` with durable payload bytes. | CT public plan case asserts `payload-bytes` exceeds 65536 and matches returned payload size | serious | project W1 | done | `wolong_backpressure_SUITE:engine_large_stdout_returns_public_plan` passed; public `payload-bytes` exceed 65536 and match payload/artifact byte counts. | `keep-artifacts=false` remains covered by existing public plan CT. |
| BH-10 | Stdout artifact over the configured stdout limit is a typed gate error and never a partial solved plan. | CT over-limit stdout fixture; assert gate/reason and absence of public solved shape | serious | artifact integrity | done | `wolong_backpressure_SUITE:stdout_over_limit_is_typed_and_recovers` passed; public result is `#(error #(engine artifact-truncated Detail))` with no solved plan. | Status fields remain machine parsed. |
| BH-11 | Noisy stderr before final `PANDAPI_STATUS` can exceed the diagnostic preview limit while Wolong still classifies from final status fields. | CT noisy-stderr fixture; assert `stderr-truncated=true` or equivalent and status fields parsed as `ok`/expected status | serious | stderr/status ownership | done | `wolong_backpressure_SUITE:noisy_stderr_preserves_final_status` passed; stderr preview is truncated and lacks status, bounded tail contains final `PANDAPI_STATUS`, status fields parse as `ok`/`solved`. | Tamper cycle proves this row. |
| BH-12 | If final status is genuinely unrecoverable under the policy, Wolong returns a typed status error rather than guessing from diagnostics or stdout. | CT fixture with missing/unrecoverable status after truncation; assert typed `missing-status`/policy-specific reason | serious | managed-process safety | done | `wolong_backpressure_SUITE:missing_final_status_is_typed_error` passed; result is `#(error #(engine missing-status Detail))`, no diagnostic prose classification. | Detail records `status-error=missing-status-line`. |
| BH-13 | Flood-then-timeout over active stdout/stderr returns a typed timeout with bounded stream data and truncation metadata. | CT fixture that writes both streams then hangs; assert timeout detail fields | serious | project W2/A6 | done | `wolong_backpressure_SUITE:flood_timeout_bounds_output_kills_group_and_recovers` passed; timeout detail caps stdout/stderr at 128 bytes and records observed bytes/truncation flags. | Uses bounded worker/process polling. |
| BH-14 | TERM-resistant flood-then-timeout children are killed as an OS process group and leave no surviving process. | CT captures `os-pid`, waits boundedly for process absence, and asserts no survivor | serious | project W2/A6 | done | Same flood-timeout CT captures `os-pid`, waits for process absence, and passes after erlexec group kill. | Preserves `kill_group`. |
| BH-15 | A successful dispatch after truncation/error/timeout still succeeds and worker cleanup returns to zero. | CT recovery sequence after over-limit and timeout cases | serious | project W4/A6 | done | Over-limit, large-artifact, and flood-timeout Slice05 CT cases run a later minimal plan and assert `wolong-dispatch-sup:worker-count` returns zero. | Recovery is through public API. |
| BH-16 | Public solved, unsolvable, timeout, and typed gate-error shapes remain compatible with Arc02/Slice03/Slice04 expectations. | run full CT; inspect representative public API cases | serious | API compatibility | done | `rebar3 as test ct` passed 87/0, including plan, pipeline, dispatch, gate, parser, exec, backpressure, and real-Chengdu suites. | Public return shapes preserved. |
| BH-17 | Fixture-backed CI remains deterministic and does not require sibling Chengdu binaries. | CI run or documented local no-env run; real-binary suite may skip when binaries are absent | correctness | CI honesty | done | GitHub Actions build `33187076539` for `b34f6bc` completed success on `ubuntu-22.04` and `macos-15`; both jobs passed compile, EUnit, CT, xref, and Dialyzer. | Remote real-binary proof remains Arc04/provisioning-dependent. |
| BH-18 | Local real-Chengdu public proof still passes when sibling binaries are available, or records a clear availability skip without claiming proof. | `WOLONG_CHENGDU_BIN_DIR=../chengdu/bin WOLONG_CHENGDU_FIXTURE_DIR=../chengdu/fixtures rebar3 as test ct --suite test/wolong_real_chengdu_SUITE.lfe` | correctness | Slice04 regression guard | done | Explicit env command passed 7/0 against sibling binaries after `b34f6bc`. | Compatibility proof, not release-scale stress. |
| BH-19 | Scope guard holds: no streaming/spooling architecture, parser `- -`, split parser workers, provisioning, public verifier, action/decomposition parser, diagnostic-prose classifier, or legacy binary fallback lands. | inspect diff and `rg` scoped terms in `src`, `test`, `README.md`, `config`, and planning docs | serious | slice scope | done | Diff for `b34f6bc` is runner/gate/config/pipeline error handling, fixture stress cases, docs, and CT. No streaming/spooling/provisioning/public verifier/parser `- -` change landed. | `rg` unavailable locally; used `find`/diff inspection. |
| BH-20 | Local gates and formatter checks pass. | `rebar3 compile`; `rebar3 as test eunit`; `rebar3 as test ct`; `rebar3 xref`; `rebar3 dialyzer`; `rebar3 lfe format --check`; `rebar3 as test lfe format --check` | correctness | repo workflow | done | All required local gates passed after `b34f6bc`: compile, EUnit 13/0, CT 87/0, xref, Dialyzer exit 0, source formatter 13 files, test formatter 23 files. | `git diff --check` also passed. |
| BH-21 | A meaningful tamper cycle proves a new hardening invariant. | break final-status preservation, artifact truncation rejection, independent stream limits, or process-group timeout; show owning CT fails; revert and show pass | serious | ledger discipline | done | Tamper changed `wolong-gate:status-stderr/1` to ignore `stderr-tail`; `noisy_stderr_preserves_final_status` failed with `missing-status`; restored helper and same focused CT passed 1/0. | Proves final-status preservation invariant. |
| BH-22 | Closing report walks every ledger row and bubbles up Arc03 close readiness or the need for a remediation slice. | inspect `closing-report.md` for BH-1 through BH-22 and Bubble-up sections | correctness | project management | done | `closing-report.md` added for Slice05 with all BH-1 through BH-22 rows and arc/project bubble-up. | CDC writes `cdc-verification.md` later. |

## What Worked

The existing in-memory artifact model remains adequate for the tested release
stress envelope once stdout and stderr get independent limits and stderr keeps
a bounded status tail. No streaming/spooling remediation slice is needed from
the Slice05 evidence.

## Closure

Proposed done by CC on 2026-08-28.
Rows: 22. Open: 0. Done: 22. Deferred: 0. No-op: 0.
